#pragma once

#include <Prerequisites.h>

class RenderTargetView {
public:
    RenderTargetView() = default;
    ~RenderTargetView() = default;

   void
   init();

   void
   update();

   void
   Destroy();

private:
    
};
